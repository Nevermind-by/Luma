name: macOS

on:
  push:
    branches:
      - main
      - 'feature/**'
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        run: |
          xcodebuild \
            -project Luma.xcodeproj \
            -scheme Luma \
            -destination 'platform=macOS' \
            MARKETING_VERSION=1.0.0 \
            CURRENT_PROJECT_VERSION=1 \
            build

      - name: Test
        run: |
          xcodebuild \
            -project Luma.xcodeproj \
            -scheme Luma \
            -destination 'platform=macOS' \
            MARKETING_VERSION=1.0.0 \
            CURRENT_PROJECT_VERSION=1 \
            test

  package-release:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: build-and-test
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Archive Release
        run: |
          rm -rf build
          xcodebuild \
            -project Luma.xcodeproj \
            -scheme Luma \
            -configuration Release \
            -destination 'generic/platform=macOS' \
            MARKETING_VERSION=1.0.0 \
            CURRENT_PROJECT_VERSION=1 \
            CODE_SIGNING_ALLOWED=NO \
            archive \
            -archivePath build/Luma.xcarchive

      - name: Package ZIP
        run: |
          mkdir -p build/package
          ditto -c -k --sequesterRsrc --keepParent \
            build/Luma.xcarchive/Products/Applications/Luma.app \
            build/package/Luma-macOS-1.0.0-unsigned.zip

      - name: Package DMG
        run: |
          hdiutil create \
            -volname 'Luma 1.0.0' \
            -srcfolder build/Luma.xcarchive/Products/Applications \
            -ov \
            -format UDZO \
            build/package/Luma-macOS-1.0.0-unsigned.dmg

      - name: Upload Release Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Luma-macOS-1.0.0-unsigned
          path: build/package/
          if-no-files-found: error
